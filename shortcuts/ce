if [ ! "$1" ] || [ "$1" = -h ]
then
  ls "$(dirname "${BASH_SOURCE[0]}")/../scripts"
else
  script="$1"
  [[ ! $script == *\.* ]] && script="${script}.sh"

  shift
  "$(dirname "${BASH_SOURCE[0]}")/../scripts/${script}" $@
fi
